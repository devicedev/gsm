
# GSM frontend

React/Vite frontend for the new GSM report.

Development:

```text
npm install
npm run dev
```

The production build is written to `DBAnalit-clean/static/gsm` and is served
by Flask at `/gsm-new/`.
